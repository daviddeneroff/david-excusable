Votes: <%= @excuse.votes.count %><br>

<a class="excuse" href="#">Vote</a> | Count:<span class="vote_count"><%=@excuse.votes.count%></span>
<input class="find-excuse" name="postid" value="<%=@excuse.id%>" type="hidden">

<h3>Comments</h3>
  <h5>Have something to say? Type it below</h5>
    <div>
      <form class="ajax_comment" action="/comment/<%= @excuse.id %>/create" method="POST">
        Content: <br>
        <textarea name="comment[content]" cols="30" rows="3"></textarea><br>
        <input type="submit" value="Make Comment">
      </form>
    </div>

  <%= @excuse.title %><br>
  <%= @excuse.content %><br>
  <% @excuse.comments.each do |comment| %>
    <%= comment.content %> by: <%= comment.user.username %><br>
  <% end %>